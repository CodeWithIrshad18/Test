2 errors :
The name 'totalActualWt' does not exist in the current context

Unexpected "{" after "@" character. Once inside the body of a code block (@if {}, @{}, etc.) you do not need to use "@{" to switch to code.
